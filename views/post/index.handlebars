<div class="card">
    <div class="card-body">
        {{#if titular}}
        <h1>{{post.title}} <span style="float: right;">De: {{titular}}</span></h1>
        {{else}}
        <h1>{{post.title}} <span style="float: right;">De: {{post.titular}}</span></h1>
        {{/if}}
        <hr>
        <small>Data de publicação: {{post.date}}</small>
        <hr>
        {{post.content}}
    </div>
</div>